# 敏感資料 — 絕對不能上 public repo
VS會議 + 績效.xlsx
*.xlsx
*.bak
# Firebase 服務帳戶金鑰(防呆:即使誤放根目錄也不會被 commit)
*firebase-adminsdk*.json
serviceAccountKey.json

# 本機工作 artifact(設計圖 / 截圖 / 草稿 / Python 工具 / 內部文件)
*.png
*.jpg
*.jpeg
待修改.txt
design_handoff_ui_styles/
scripts/*
!scripts/tests/
# ↑ scripts/ 底下只有 tests/ 納入版控(純邏輯測試、零病歷號、不連 Firebase);
#   金鑰 / backups / 含病歷號的匯入 JSON 一律排除。
#   注意:必須寫 scripts/* 而非 scripts/ —— 父目錄被整個排除時,! 例外不會生效。
CLAUDE.md
DECISION_LOG.md
SYNC_REVIEW_*.md
registry_plan.md
registry_design.html
database.rules.json
# 防呆:含病歷號的 parser 暫存/輸出 + Worknum 匯出絕不上 repo
# _smoke*.html = 冒煙測試頁,內嵌整份備份資料(含病歷號)→ 絕不可 commit
_smoke*.html
_test_*.html
_repro_*.html
scratchpad_*.txt
scratchpad/
*_parsed.json
*_export.json
worknum_export.json
_mock_days.json
*_mock*.json
*_import.json
registry_import.json

# Claude 內部
.claude/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
